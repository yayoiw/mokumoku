    <div class="d-flex align-items-end justify-content-between bg-white px-4 pt-2 pb-4 rounded-none rounded-bottom-md shadow-sm">
      <div class="d-flex align-items-center">
        <div class="me-2 position-relative d-flex justify-content-end align-items-end mt-n5">
          <%= image_tag current_user.decorate.avatar, class: 'avatar-xl rounded-circle border border-4 border-white' %>
        </div>
        <div class="lh-1">
          <h2 class="mb-0">
            <%= current_user.name %>
          </h2>
          <p class="mb-0 d-block"><%= current_user.email %></p>
          <p class="mb-0 d-block"><%= current_user.profile %></p>
          <p class="mb-0 d-block"><%= current_user.hobby %></p>
        </div>
      </div>
    </div>